import styles from '../page.module.css';

export default function Header() {
    return (
        <header className={styles.header}>
            <span>Exclusivo para aqueles que estão dispostos a uma mudança</span>
        </header>
    )
}